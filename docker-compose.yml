version: '3'
services:
  app-be:
    build: ./backend
    image: antonybudianto/app-be
  app-fe:
    build: ./frontend
    image: antonybudianto/app-fe
    ports:
      - 3000:80
    depends_on:
      - app-be
